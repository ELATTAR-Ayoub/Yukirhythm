/**
 * The real Home screen on real data. The screen component is unchanged — it
 * reads feeds from the studio context, which the real StudioProvider (this
 * route's layout) fills from /api/feed. Increment 5 moves it to the app root.
 */
export { default } from "@/app/design-system/screens/(app)/home/page";
